# Only needed for deploying to Modal
modal>=0.63.0

# All other dependencies (torch, transformers, qwen-tts, etc.)
# are automatically installed in the Modal container image.
# See app.py lines 16-38 for the full dependency list.
